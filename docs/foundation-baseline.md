# Foundation hardening — baseline (Phase 0)

Last verified: `npm test`, `npm run lint`, and `npm run build` all pass (100 tests) before implementation work on the roadmap.

Use this as a pre/post check when completing each phase.
